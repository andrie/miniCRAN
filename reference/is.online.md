# Returns TRUE if the p3m URL can be accessed.

Returns TRUE if the p3m URL can be accessed.

## Usage

``` r
is.online(url = NULL, tryHttp = TRUE)
```

## Arguments

- url:

  p3m url

- tryHttp:

  If TRUE, also attempts http URL, for compatibility with older versions
  of R
